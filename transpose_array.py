#########################
import zipimport

# FAILS
# FAILS
# FAILS
# FAILS
# FAILS

array = [['a', 'b'], ['c', 'd'], ['e', 'f']]
print(array)
transposed = ""
transposed = zip(array)

print(transposed) # [('a', 'c', 'e'), ('b', 'd', 'f')]
print ("Done")